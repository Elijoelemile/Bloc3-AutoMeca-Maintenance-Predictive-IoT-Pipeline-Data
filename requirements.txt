# Extraction / chargement
kaggle>=1.6
boto3>=1.34
python-dotenv>=1.0
pyarrow>=15.0

# Streaming
paho-mqtt>=2.0
confluent-kafka>=2.4

# Bases de donnees
psycopg2-binary>=2.9
clickhouse-connect>=0.7

# Orchestration
# Installer avec le fichier de contraintes officiel (evite les conflits
# de dependances) : pip install apache-airflow==2.10.3 --constraint
# https://raw.githubusercontent.com/apache/airflow/constraints-2.10.3/constraints-3.12.txt
apache-airflow>=2.9

# Tests
pytest>=8.0
